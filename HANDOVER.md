# 交接文档 / Handover — 项目 927

> 记录时间：2026-09-17，更新于 2026-09-18
> 状态：**两轮实验均已完成。当前有效结果是第 2 轮的 unified 模型。**
> 用途：自己学习 + 随时可恢复执行。
>
> 📊 完整实验数据见 [`RESULTS.md`](RESULTS.md)

---

## 0. 最新进展（2026-09-18 更新）

### ⚠️ 最重要的一件事：第 1 轮的 mAP50 = 0.9266 是虚高的

实测发现 **PKU-Market-PCB 只有 10 块 PCB 母板**，每块母板合成 6 类缺陷。同母板的图在非缺陷区域**逐像素完全相同**（实测 0.00 灰阶差、0.0% 差异像素）。而第 1 轮用的是按类别随机划分，导致**同一块板同时进了 train 和 test** —— 模型可能是在认板子而不是认缺陷。

**已修复**：unified 数据集改为按母板 block 划分，并加了泄漏自检。**第 1 轮的数字不要再用于论文。**

### 本轮新增

| 内容 | 结果 |
|---|---|
| 数据集 B | **PCB-Defect 2025**（Mendeley DOI `10.17632/vdj74sngvn.1`，CC BY 4.0，230 张，真实化学蚀刻缺陷） |
| 类别统一 | **7 类**。`missing_hole` 与 `missing_pad` 语义不同，**未合并**（已切图目视确认） |
| unified 数据集 | train 641 / val 124 / **test_pku 123 / test_pcb2025 35**（两个独立测试集） |
| unified 训练 | mAP50 **0.8214**（最优 ep62，早停于 ep92，58.4 分钟） |
| test_pku | mAP50 **0.8488** |
| test_pcb2025 | mAP50 **0.7743** |
| 时延拆解 | 🔴 **58% 花在 JPEG 解码，模型推理只占 22%** |

### 🔴 对第二阶段最关键的发现

**分辨率几乎不影响本地时延**（320→640 像素翻 4 倍，时延仅 23.9→27.9 ms），
**但强烈影响传输量**（22.3→67.7 KB，3 倍）。

→ **分辨率管"带宽"，放置管"时延"。** 若按最初设想用降分辨率省本地时延，收益极小、精度代价极大（320 时 mAP50 掉到 0.48）。

### 新增命令

```bash
python -m pcbvis unified       # 下载 PCB-Defect 2025 + 构建统一数据集
python -m pcbvis profile       # 每个 imgsz 同时测 精度+时延+JPEG字节
python -m pcbvis latency       # 交错法只测时延（抗热漂移）
python -m pcbvis train --data data/unified/data.yaml --name unified_yolo11n
```

### 两套数据的注意点

- **PKU 缺陷是 Photoshop 合成的**，PCB2025 是**真实化学蚀刻**的，两者结论不能直接外推到产线。
- **test_pcb2025 只有 35 张**，指标方差大。
- 两个测试集都在 **imgsz=608** 达峰而非 640 —— 可能是噪声，需多 seed 才能定论（**这是推测，未证实**）。

---

---

## 1. 当前状态快照（准确数字）

| 项目 | 状态 |
|---|---|
| Python 环境 | ✅ 完成，`.venv/`（Python 3.14.0） |
| 依赖安装 | ✅ 完成，torch 2.14.0 / ultralytics 8.4.154 / opencv 5.0.0 |
| MPS 加速 | ✅ 可用（实测 `torch.backends.mps.is_available() == True`） |
| 代码模块 | ✅ `pcbvis/` 下 8 个模块全部写完 |
| 预训练权重 | ✅ `models/yolo11n.pt`（5.4 MB） |
| **数据集下载** | ✅ **693/693 张**（约 970 MB） |
| **格式转换** | ✅ **2953 个缺陷框**，`data/pcb_yolo/` 已生成 |
| 模型微调 | ❌ **尚未开始 ← 下一步** |
| 评估 / 推理 | ❌ 尚未开始 |

### 数据转换的实测结果

```
有效图片      : 693
缺陷框总数    : 2953          （平均 4.26 框/图）
训练/验证/测试: 483 / 102 / 108
各类别框数    : missing_hole 497 | mouse_bite 492 | open_circuit 482
                short 491 | spur 488 | spurious_copper 503
缺陷框宽度    : 中位数 2.37% 图宽（3034px 上约 72px）
                最小 0.79% | 最大 10.80%
```

**独立核验已通过**（不只是信转换脚本自己打印的报告）：逐图检查过图片与标签一一配对、无孤立文件、每行 5 列、类别 id 全在 0~5、所有归一化坐标都在 `[0,1]` 内 —— **零问题**。同时也说明 693 个 XML 全部配对成功、且 XML 声明的尺寸与实际图片尺寸**完全一致**（否则会打印警告）。

### 已实测验证过的事（不是推测）

1. **依赖全装上了** —— 原本担心 Python 3.14 太新没有轮子，实际逐个核对 PyPI 后确认 torch/torchvision/opencv 都有 arm64 轮子，实测安装成功。
2. **VOC→YOLO 解析逻辑正确** —— 先用合成 XML 跑通并与手工核算逐位比对，再在全部 693 张真实数据上跑通。
3. **MPS 训练链路能跑** —— 用合成的极小数据集在 MPS 上跑了 1 个 epoch，YOLO11n 正常训练+验证。
4. **数据本身干净** —— 见上面的独立核验。

### 关于下载中断（已解决，留作记录）

第一次下载在第 83 张时遇到 `httpx.RemoteProtocolError: Server disconnected without sending a response.` —— HuggingFace 服务端的偶发断连，不是代码问题。

**已修好**：`pcbvis/data_prep.py` 的 `download()` 带自动重试（最多 8 次，退避 2/4/8…20 秒），并发降到 4，并用 `configs/pcb.yaml` 里的 `expected_images: 693` 判断是否真的下完。重入即断点续传 —— 这次就是靠它从 83 张直接续完的。

---

## 2. 下一步怎么做

数据已经就绪，**直接训练即可**：

```bash
# 在项目根目录下执行
# 微调（M4 上约 30~60 分钟）
.venv/bin/python -m pcbvis train

# 评估
.venv/bin/python -m pcbvis eval

# 推理出带框图
.venv/bin/python -m pcbvis predict --imgsz 640
```

**判断训练成功的标志**：结束时 mAP50 **明显 > 0.5**。若接近 0，先回头查标注转换和类别映射，不要急着调参。

> 可选清理：`rm -rf /tmp/pcbvis_trainsmoke /tmp/pcbvis_trainout`（冒烟测试的临时目录）

---

## 3. 代码导读（建议按这个顺序读）

整个包在 `pcbvis/`，界面/输出用英文，注释中文。建议阅读顺序：

### ① `paths.py` —— 先看这个
所有路径的唯一来源。看懂它就知道数据、权重、结果分别落在哪。

### ② `config.py` + `configs/pcb.yaml` —— 再看配置
类别表、划分比例、随机种子都在 YAML 里。**改类别或换数据集只改这里**。

### ③ `data_prep.py` —— 本阶段最核心的模块
做四件事：下载 → 解析 XML → 分层划分 → 写 YOLO 格式。重点读 `_parse_one()`，它把 VOC 的绝对像素坐标转成 YOLO 的归一化 `cx cy w h`。

**里面藏着两个真实的坑，注释里也标了：**
- XML 里的 `<filename>` 字段（`01_missing_hole_01.jpg`）**和实际文件名（`missing_hole01.jpg`）对不上**。所以配对只能按文件名词干做，信 `<filename>` 会直接崩。
- 图像尺寸以**图片实际尺寸**为准（用 PIL 读），XML 的 `<size>` 只用来交叉校验，不一致会打印警告。

### ④ `train.py`
微调。`resolve_device()` 自动挑 MPS/CUDA/CPU；`ensure_weights()` 把预训练权重显式下到 `models/`，避免 ultralytics 把 `.pt` 撒在项目根目录。

### ⑤ `predict.py` —— 第二阶段的关键
推理并把**逐图时延**记进 JSON。注意 `--imgsz` 是一级参数，还有预热逻辑（首次推理含加载开销，不计时），以及把 `imgsz/时延/检出数` 直接画在输出图上，方便肉眼对比。

### ⑥ `evaluate.py`
出 mAP 指标，逐类也存。`--imgsz` 同样可调。

### ⑦ `cli.py` / `__main__.py`
命令行入口，把上面几个串起来。额外提供 `sweep` 子命令，一次跑多个分辨率出对比表。

---

## 4. 关于数据集（务必记住）

**`RobotHuman/PCB_defect`** —— 即经典 **PKU-Market-PCB**（Huang et al., 2019）。

- 693 张图，6 类：`missing_hole` `mouse_bite` `open_circuit` `short` `spur` `spurious_copper`
- 分辨率 **3034×1586**，每图 3~5 个缺陷框
- 标注是 Pascal VOC XML，代码自动转 YOLO
- MIT 许可，HuggingFace 公开下载，**不需要登录或 API key**
- 该镜像只有 693 张，约为原数据集（1386 张）的一半

### ⚠️ 论文里必须写明的两点

1. **缺陷是合成的。** 数据集作者用 Photoshop 把缺陷贴到真实 PCB 照片上（数据集自述），**不是产线真实缺陷**。这直接影响结论的泛化性，不能含糊过去。
2. **缺陷极小。** 实测中位宽度仅占图宽 **2.34%**（3034px 上约 71px）。

### 为什么第 2 点对本项目是好消息

缺陷小意味着**推理分辨率一降，小缺陷最先检不出来**。这让 `imgsz` 成为一个真实有效的权衡旋钮。

图片是 3034×1586（宽高比 1.91），YOLO 会 letterbox 到正方形，所以缩放到 `imgsz` 时横向缩放比是 `imgsz / 3034`：

| imgsz | 中位缺陷 (72px) 缩放到 | 最小缺陷 (24px) 缩放到 | 预期 |
|---|---|---|---|
| 320 | ~7.6 px | **~2.5 px** | 严重漏检，但快、传输字节少 |
| 640 | ~15 px | ~5 px | 折中 |
| 1280 | ~30 px | ~10 px | 召回最全，但慢、字节多 |

注意**最小缺陷那一列**：在 imgsz=320 时只有约 2.5 像素 —— 基本不可能被检出。这预示 `sweep` 出来的精度曲线会在低分辨率端掉得很厉害，而**这个差距正是自适应策略的收益来源**。

**这个精度差异是数据本身的性质，不是人为编出来的** —— 这正是第二阶段「分辨率 / 时延 / 带宽」三方权衡的实验基础。`sweep` 子命令就是用来把这个曲线量出来的。

---

## 5. 未验证的部分（下一步要确认的）

| 待确认 | 怎么确认 | 期望 |
|---|---|---|
| ~~真实数据的转换正确~~ | ✅ **已验证** | 693 图 / 2953 框，独立核验零问题 |
| ~~标签文件格式对~~ | ✅ **已验证** | 每行 5 列，坐标全在 `[0,1]`，类别 id 全合法 |
| **微调真的学到东西** | `train` 结束时的 mAP50 | **明显 > 0.5**；若接近 0 说明转换或类别映射有 bug |
| **框真的套在缺陷上** | 打开 `results/predict/imgsz640/` 的图 | 框落在缺陷位置，不是随机位置 |

> **最关键的一条**：mAP50 若接近 0，别急着调参，先回头查标注转换和类别映射 —— 这类问题几乎都出在数据转换，不在模型。（数据这一侧现在已经核验干净了，所以真出问题更可能在类别映射或训练超参上。）

---

## 6. 第二阶段设计思路（尚未实现，供预习）

第一阶段刻意把 `imgsz` 和逐图时延暴露出来，就是为了第二阶段。计划：

1. **网络模拟器** —— 可配带宽 / RTT / 抖动 / 丢包。关键设计：**传输字节数是真的**（真走 HTTP 发出去），**时延是按模型算的**（本地回环真实延迟只有微秒级，必须模拟）。这是这类研究的标准做法，论文里要讲清楚。
2. **edge server** —— 独立进程起 HTTP 推理服务（用 `uvicorn` 独立进程，**别用 multiprocessing fork**，MPS 在 fork 后容易挂），可模拟服务器负载。
3. **自适应策略** —— 核心。维护本地推理耗时、服务器耗时、RTT、可用带宽的在线估计；对每个候选动作（本地/卸载 × 各分辨率）预测时延与精度，在满足精度下限和截止时间的前提下选最优。精度预测靠第一阶段的 `sweep` 曲线标定。
4. **对比实验** —— local-only / edge-only / 固定卸载 / 自适应，在不同网络条件下比：精度、端到端时延、截止时间达成率、传输数据量。
5. **演示界面** —— 实时显示检测框和系统决策（英文界面）。

---

## 7. 自学问题清单

想真正搞懂这套代码，试着回答：

1. `_parse_one()` 里为什么用 PIL 读图片尺寸，而不是直接用 XML 里的 `<size>`？两者不一致时会发生什么？
2. YOLO 的标签为什么是归一化的 `cx cy w h` 而不是绝对 `xmin ymin xmax ymax`？归一化带来什么好处？
3. 划分数据时为什么按类别分层，而不是把所有图混在一起随机分？
4. `predict.py` 里为什么要先做一次「预热」推理？不预热会怎样？（提示：看 `latency_ms` 的 min 和 max 差距）
5. 缺陷占图宽 2.34% 这个事实，会怎么影响 imgsz=320 时的召回率？为什么？

---

## 8. 常用命令速查

```bash
# 激活环境（或直接用 .venv/bin/python 前缀）
source .venv/bin/activate

# 冒烟测试：每类只取几 张，快速验证流程
python -m pcbvis prepare --limit 5
python -m pcbvis prepare --no-download     # 只重做转换，不重新下载

# 训练相关
python -m pcbvis train --epochs 50 --batch 8 --device mps
python -m pcbvis train --workers 0          # macOS 上 DataLoader 卡住时用

# 看不同分辨率的效果
python -m pcbvis sweep --imgsz 320,416,640,1280
python -m pcbvis predict --imgsz 320 --limit 10

# 完整帮助
python -m pcbvis -h
python -m pcbvis train -h
```

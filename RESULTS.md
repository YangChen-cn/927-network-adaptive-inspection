# 训练结果汇总

> 本文件记录两轮实验：
> **第 1 轮（PKU-only）** —— 见第 1~10 节。⚠️ **该轮的 mAP50 已确认虚高，见下方勘误。**
> **第 2 轮（Unified）** —— 见第 11 节起，是当前有效的结果。

---

## ⚠️ 勘误：第 1 轮的 mAP50 = 0.9266 因数据泄漏而虚高

**发现（2026-09-18，实测）：** PKU-Market-PCB 只有 **10 块 PCB 母板**，每块母板用 Photoshop 合成 6 类缺陷各若干张。

验证方法：把两张图的缺陷框区域都遮掉，只比较非缺陷区域的像素。

| 对比 | 平均灰阶差 | 差异像素占比 |
|---|---|---|
| 跨类别·同序号（`missing_hole01` vs `mouse_bite01`） | **0.00** | **0.0%** |
| 同类别·相邻序号（01 vs 02） | **0.00** | **0.0%** |
| 同类别·远隔序号（01 vs 50） | 21.65 | 66.2% |

即 `missing_hole01` 与 `mouse_bite01` **是同一块板的照片**，只贴了不同缺陷。

**问题：** 第 1 轮用的是「按类别文件夹分层随机划分」，结果同一块母板同时进了 train 和 test。**模型可能是在认板子而不是认缺陷。**

**因此 0.9266 不能作为泛化性能使用。** 第 2 轮的 unified 数据集已改为按母板 block 划分（整块板只进一个 split），并加了泄漏自检。

> 母板分块边界（实测，6 个类别目录一致）：21, 41, 51, 61, 71, 81, 91, 96, 106 —— 共 10 块。

---

# 第 1 轮：PKU-only（数据划分有泄漏，数字仅供参考）

> 运行时间：2026-09-17 | 设备：Apple M4 (MPS) | 权重：`results/train/pcb_yolo11n/weights/best.pt`
> 数据：`RobotHuman/PCB_defect`（PKU-Market-PCB），train/val/test = 483/102/108

---

## 1. 一句话结论

**训练成功。** 验证集 mAP50 = **0.9266**，远超「mAP50 > 0.5」的合格线，说明标注转换与类别映射完全正确，模型确实学到了 PCB 缺陷特征。

---

## 2. 训练配置

| 项 | 值 |
|---|---|
| 模型 | YOLO11n（COCO 预训练微调） |
| epochs | 100（跑满，patience=30 **未触发早停**） |
| imgsz / batch | 640 / 16 |
| 设备 | MPS (Apple M4) |
| 优化器 | auto → lr0 0.01, lrf 0.01 |
| 数据增强 | mosaic 1.0、scale 0.5、fliplr 0.5、hsv_s 0.7、hsv_v 0.4、erasing 0.4、close_mosaic 10 |
| 随机种子 | 42 |
| 模型规模 | 2,583,322 参数 / 6.4 GFLOPs |

---

## 3. 训练耗时

| 指标 | 值 |
|---|---|
| 总耗时 | **50.9 分钟** |
| 每 epoch | 30.5 秒（平均） |

> 参考：M4 Pro 上同类任务约 23 分钟；M4 基础款慢约一倍属正常。

---

## 4. 训练进程

| epoch | mAP50 | mAP50-95 | Recall |
|---|---|---|---|
| 1 | 0.0000 | 0.0000 | 0.0044 |
| 10 | 0.5233 | 0.2267 | 0.4826 |
| 25 | 0.8174 | 0.3861 | 0.7207 |
| 50 | 0.8791 | 0.4374 | 0.7929 |
| 75 | 0.9197 | 0.4639 | 0.8852 |
| **87（最优）** | **0.9262** | **0.4886** | **0.8869** |
| 100 | 0.9203 | 0.4707 | 0.8624 |

**读法**：前 10 个 epoch 就冲到 0.52，说明预训练权重迁移得很好；25 epoch 到 0.82；之后是缓慢爬升。最优轮在 epoch 87，最后 13 轮没有提升 —— 说明**已经收敛**，再训下去收益很小。

注意末轮（0.9203）比最优轮（0.9262）略低，这是正常的训练波动，所以最终用的是 `best.pt` 而不是 `last.pt`。

---

## 5. 最终指标（val 集，102 张 / 446 个缺陷框）

| 指标 | 值 |
|---|---|
| **mAP50** | **0.9266** |
| mAP50-95 | 0.4905 |
| Precision | 0.9366 |
| Recall | 0.8907 |

### 逐类 mAP50

| 类别 | mAP50 | |
|---|---|---|
| missing_hole | **0.9867** | ████████████████████████████████████████ |
| short | 0.9612 | ██████████████████████████████████████ |
| open_circuit | 0.9438 | █████████████████████████████████████ |
| spur | 0.9210 | █████████████████████████████████████ |
| spurious_copper | 0.8806 | ███████████████████████████████████ |
| mouse_bite | 0.8664 | ██████████████████████████████████ |

**读法**：最好（missing_hole 0.987）与最差（mouse_bite 0.866）相差 12 个百分点，跨度不大，说明**没有哪一类完全学不会**。`mouse_bite` 是「板边被咬掉一块」，形态与正常板边最接近，最难区分；`missing_hole` 是「该有孔的地方没孔」，视觉特征最显著，所以最好学。

---

## 6. 推理速度（imgsz=640，MPS）

| 阶段 | 耗时 |
|---|---|
| 预处理 | 0.2 ms |
| **推理** | **9.2 ms** |
| 后处理 | 3.2 ms |

> **这个数字第二阶段会反复用到**：它是「本地推理耗时」的实测基准。9.2ms 看起来很快，但那是 M4 跑 nano 模型的结果 —— 第二阶段的「资源受限设备」要靠降分辨率或模拟降速来体现。

---

## 7. 产物位置

```
results/train/pcb_yolo11n/
├── weights/best.pt               ← 最优权重（0.9262）
├── results.csv                   ← 逐 epoch 指标（画曲线用这个）
├── results.png                   ← 损失/精度总览曲线    ← 汇报首选
├── confusion_matrix.png          ← 混淆矩阵
├── confusion_matrix_normalized.png
├── BoxPR_curve.png               ← PR 曲线
├── BoxF1_curve.png  BoxP_curve.png  BoxR_curve.png
├── val_batch{0,1,2}_pred.jpg     ← 验证集预测可视化 ← 看框准不准用这个
└── val_batch{0,1,2}_labels.jpg   ← 对应的真值

results/eval/metrics_imgsz640_val.json   ← 本次评估的机读结果
```

**给导师看的话，优先拿这三张**：`results.png`（训练曲线）、`confusion_matrix_normalized.png`（分类表现）、`val_batch0_pred.jpg`（实际检测效果）。

---

## 8. 必须写进论文的注意事项

1. **缺陷是合成的** —— 数据集作者用 Photoshop 贴到真实 PCB 照片上，**不是产线真实缺陷**，泛化性结论要保守。
2. **该镜像只有 693 张**（原数据集 1386 张的一半），训练集仅 483 张。
3. **val 集只有 102 张图**，所以指标存在一定方差，小数点后第二位不必过度解读。
4. **mAP50-95 只有 0.49**，比 mAP50 低不少 —— 说明**框的位置还不够精确**。这对缺陷检测其实可以接受（能定位到缺陷区域即可），但如果论文要报定位精度，需要如实说明。
5. 这是**单一数据集、单一随机种子**的结果，还没做多 seed 重复实验。

---

## 9. 精度 vs 分辨率（核心实验）

```bash
.venv/bin/python -m pcbvis sweep --imgsz 320,416,640,1280
```

val 集（102 张 / 446 个缺陷框），`best.pt` 训练于 **imgsz=640**：

| imgsz | mAP50 | mAP50-95 | Precision | Recall | 相对 640 |
|---|---|---|---|---|---|
| 320 | 0.4050 | 0.1643 | 0.6717 | 0.3723 | **−0.5216** |
| 416 | 0.7257 | 0.3358 | 0.8050 | 0.7022 | −0.2009 |
| **640** | **0.9266** | **0.4905** | **0.9366** | **0.8907** | — |
| 1280 | 0.8539 | 0.3945 | 0.8462 | 0.7956 | −0.0727 |

### 🔴 重要：曲线不是单调的，1280 反而比 640 差

**这是一个反直觉但关键的结果。** 直觉上分辨率越高精度越高，但实测 **1280 比 640 低 7.3 个百分点**。

**最可能的解释：训练/推理分辨率不匹配。** 模型只在 640 上训练过（训练时的 `scale=0.5` 增强意味着它见过的尺度约在 320~960 之间），推理时喂 1280 超出了它学过的尺度范围，特征统计对不上，精度反而掉。这是检测模型已知的 scale sensitivity 现象。

> ⚠️ 这是**解释（假设）**，不是已证实的结论。要证实需要对照实验：重新在 1280 上训练一次，如果那时 1280 变成最优，就说明确实是尺度不匹配导致的。

### 逐类 mAP50

| 类别 | 320 | 416 | 640 | 1280 |
|---|---|---|---|---|
| open_circuit | 0.2292 | 0.5687 | **0.9438** | 0.9296 |
| missing_hole | 0.7281 | 0.9836 | **0.9867** | 0.9131 |
| mouse_bite | 0.3294 | 0.6606 | 0.8664 | **0.8755** |
| spur | 0.3108 | 0.7104 | **0.9210** | 0.8451 |
| short | 0.5140 | 0.8286 | **0.9612** | 0.8431 |
| spurious_copper | 0.3186 | 0.6023 | **0.8806** | 0.7172 |

**读法（这部分很好写进论文）**：各类缺陷对分辨率的敏感度差别很大，且**和缺陷的视觉尺度直接相关**——

- `missing_hole`（该有孔的地方没孔）在 320 下仍有 **0.728**，是唯一一个低分辨率下还能用的类别。因为「孔」是个大而显著的特征，缩小了也还在。
- `open_circuit` 掉得最惨：**0.944 → 0.229**，几乎归零。断路是细线状缺陷，降到 320 后线宽只剩几个像素，直接消失。
- 其余细结构缺陷（`spur` `spurious_copper` `short`）同样在 320 崩掉（0.31~0.51）。

### 对第二阶段（自适应策略）的三个直接结论

1. **动作空间应限制为 {320, 416, 640}，把 1280 排除。** 1280 是**严格劣解**：算力更贵、传输字节更多（约 4 倍），精度还更低。把它留在候选集里只会让策略浪费决策。
2. **分辨率的收益/代价非常陡峭**：320→640 精度翻了 2.3 倍（0.405→0.927）。这意味着**网络差的时候硬扛低分辨率，代价极大**——正是「卸载到边缘跑高分辨率」有意义的场景。
3. **在线精度预测可以按类别做**：不同类缺陷的精度-分辨率曲线差很多，如果后续做按需决策，用「图中最可能出现的缺陷类型」来选分辨率会比全局平均更准。

---

## 10. 下一步

**已完成**：数据 → 微调 → 评估 → 分辨率扫描（第一阶段全部完成）。

**第二阶段建议**：

- **先做对照实验证实上面的解释**：在 imgsz=1280 上重训一次，看 1280 是否变成最优。这个实验成本低（约 1 小时），但能把「尺度不匹配」从假设变成结论，也是论文里一个漂亮的消融。
- 网络模拟器 + edge server（独立进程，**不要用 multiprocessing fork**，MPS 在 fork 后会挂）。
- 自适应策略：动作空间 {本地, 卸载} × {320, 416, 640}。
- 对比实验：local-only / edge-only / 固定卸载 / 自适应。

**推理可视化**（尚未运行）：
```bash
.venv/bin/python -m pcbvis predict --imgsz 640
.venv/bin/python -m pcbvis predict --imgsz 320   # 对比看漏检
```

---
---

# 第 2 轮：Unified（PKU + PCB-Defect 2025）—— 当前有效结果

## 11. 两个数据集的原始类别

| | PKU-Market-PCB | PCB-Defect 2025 |
|---|---|---|
| 来源 | `RobotHuman/PCB_defect` (HuggingFace) | Mendeley, DOI `10.17632/vdj74sngvn.1` |
| 许可 | MIT | **CC BY 4.0** |
| 图片数 | 693 | 230 |
| 标注数 | 2953 | 1704 |
| 标注格式 | Pascal VOC XML | **COCO JSON** |
| 分辨率 | 统一 3034×1586 | 800×600 ~ 6000×4000（230 张尺寸**全不同**） |
| 缺陷来源 | **Photoshop 合成** | **化学蚀刻真实缺陷** |
| 原始类别 | `missing_hole`, `mouse_bite`, `open_circuit`, `short`, `spur`, `spurious_copper` | `missing_pad`, `mouse_bite`, `open_circuit`, `short`, `spur`, `spurious_copper` |

> PCB-Defect 2025 的 COCO 里 `id=0` 是 `detecting-pcb-defects`（supercategory 占位，0 个标注），真正类别是 id=1..6。**原始 id 一律不能沿用**，必须重映射。

## 12. 类别统一：7 类（`missing_hole` 与 `missing_pad` 不合并）

**核实方式不是看名字，是两条独立证据：**

1. **数据集自述**：PCB-Defect 2025 明确写的是 "missing pad"。
2. **切图视觉比对**（已实际执行）：
   - PKU `missing_hole`：图中其他焊盘都有可见的**金属化钻孔**，出问题那个焊盘整个是平的、**缺孔**
   - PCB-Defect 2025 `missing_pad`：走线末端的**铜焊盘环**不完整，**缺的是焊盘**

孔 ≠ 焊盘，是 PCB 制造中两种不同特征，**不能强行合并**。

其余 5 类两个数据集**命名完全一致**（都用下划线风格），直接合并。

```
unified id  名称               PKU   PCB2025
    0       missing_hole        ✓      —
    1       missing_pad         —      ✓
    2       mouse_bite          ✓      ✓
    3       open_circuit        ✓      ✓
    4       short               ✓      ✓
    5       spur                ✓      ✓
    6       spurious_copper     ✓      ✓
```

完整 mapping 存于 `data/unified/class_mapping.json`（含原始类别→统一 id、划分方式、母板分组），可供论文追溯。

## 13. 防泄漏划分

| 数据集 | 划分方式 | 依据 |
|---|---|---|
| PKU | **按 10 块母板 block 划分**（整块板只进一个 split） | 同母板图非缺陷区逐像素相同，随机划分会泄漏 |
| PCB-Defect 2025 | 按图片随机（seed=42） | 230 张尺寸全不同，各自独立，无同源图 |

两个数据集**各自先划分再合并**：

| split | PKU | PCB2025 | 合计 |
|---|---|---|---|
| train | 480 | 161 | **641** |
| val | 90 | 34 | **124** |
| test_pku | 123 | 0 | **123** |
| test_pcb2025 | 0 | 35 | **35** |

**泄漏自检：`train 与 test 共用的母板分组 = 无`** ✅

标签格式核验：4657 个框，逐行检查列数、类别 id 范围、坐标 `[0,1]` —— **零问题**。

## 14. Unified YOLO11n 训练

| 项 | 值 |
|---|---|
| 模型 | YOLO11n（COCO 预训练） |
| 数据 | `data/unified/data.yaml`（7 类，train 641 / val 124） |
| epochs | 100（**实际 92 触发早停**，最优 epoch 62） |
| imgsz / batch | 640 / 16 |
| 设备 | MPS (Apple M4) |
| 耗时 | 58.4 分钟（38.1 秒/epoch） |
| 增强 | mosaic 1.0 / scale 0.5 / fliplr 0.5 / close_mosaic 10（ultralytics 默认） |

**训练结果：**

| | epoch | mAP50 | mAP50-95 | P | R |
|---|---|---|---|---|---|
| 首轮 | 1 | 0.0001 | 0.0000 | 0.0002 | 0.0068 |
| **最优** | **62** | **0.8214** | **0.3970** | **0.8087** | **0.7517** |
| 末轮 | 92 | 0.7896 | 0.3733 | 0.7329 | 0.7733 |

产物：`results/train/unified_yolo11n/`（best.pt / last.pt / run_config.json / results.csv / 曲线 / 混淆矩阵）

## 15. 两个独立测试集的结果

**没有合并成一个 overall** —— 合并会掩盖 domain 差异。

### test_pku（123 张，PKU 域）

| 指标 | 值 |
|---|---|
| mAP50 | **0.8488** |
| mAP50-95 | 0.3990 |
| Precision | 0.8180 |
| Recall | 0.7873 |

### test_pcb2025（35 张，PCB-Defect 2025 域）

| 指标 | 值 |
|---|---|
| mAP50 | **0.7743** |
| mAP50-95 | 0.3839 |
| Precision | 0.8670 |
| Recall | 0.6814 |

### 逐类 mAP50 @ imgsz=640

| 类别 | test_pku | test_pcb2025 | 解读 |
|---|---|---|---|
| missing_hole | **0.9327** | — | 仅 PKU 有 |
| missing_pad | — | **0.7814** | 仅 PCB2025 有；仅 206 个训练框仍能学到 |
| mouse_bite | 0.8489 | **0.4163** | ⚠ **跨域迁移最差** |
| open_circuit | 0.8348 | 0.8390 | 迁移良好 |
| short | 0.9279 | 0.8629 | 迁移良好 |
| spur | 0.8746 | 0.7880 | 迁移中等 |
| spurious_copper | **0.6740** | **0.9583** | 反向：在 PCB2025 上反而更好 |

**unified model 是否同时适应两个 domain？—— 部分适应。**

- 7 类中有 5 类在两个域都 >0.78，说明共享的视觉特征确实被学到了一套。
- **`mouse_bite` 是明确的失败案例**（0.849 → 0.416）。很可能因为两个数据集的该缺陷成因完全不同：PKU 是 PS 合成，PCB2025 是化学蚀刻，外观差异大。
- `spurious_copper` 出现**反向迁移**（PKU 0.674 < PCB2025 0.958），同样指向两个域对该缺陷的定义/外观不一致。
- `missing_pad` 只有 206 个训练框却拿到 0.78，说明共享 backbone 帮助了它——这是多数据集合并的正面收益。

## 16. 分辨率 sweep（两个测试集各自一套）

`results/profile/unified_{pku,pcb2025}_test_imgsz*.json`，曲线图 `results/profile/curves_unified_*.png`

### test_pku

| imgsz | mAP50 | mAP50-95 | P | R | 时延ms | JPEG_KB |
|---|---|---|---|---|---|---|
| 320 | 0.4767 | 0.1480 | 0.5985 | 0.5127 | 23.9 | 22.3 |
| 384 | 0.6713 | 0.2271 | 0.8232 | 0.6266 | 24.3 | 30.3 |
| 416 | 0.6889 | 0.2561 | 0.8636 | 0.5972 | 24.9 | 34.3 |
| 448 | 0.7561 | 0.2949 | 0.8151 | 0.7087 | 25.4 | 38.7 |
| 480 | 0.8131 | 0.3358 | 0.8506 | 0.7433 | 25.7 | 42.8 |
| 512 | 0.8535 | 0.3415 | 0.9278 | 0.7607 | 26.2 | 47.7 |
| 544 | 0.8422 | 0.3651 | 0.9136 | 0.7568 | 26.6 | 52.7 |
| 576 | 0.8760 | 0.3882 | 0.9390 | 0.8155 | 27.1 | 57.6 |
| **608** | **0.8870** | 0.4032 | 0.9336 | 0.8213 | 27.7 | 63.0 |
| 640 | 0.8488 | **0.3990** | 0.8180 | 0.7873 | 27.9 | 67.7 |

### test_pcb2025

| imgsz | mAP50 | mAP50-95 | P | R | 时延ms | JPEG_KB |
|---|---|---|---|---|---|---|
| 320 | 0.3709 | 0.1551 | 0.5220 | 0.3883 | 22.6 | 11.5 |
| 384 | 0.5005 | 0.2315 | 0.6805 | 0.4556 | 22.9 | 15.6 |
| 416 | 0.6203 | 0.2858 | 0.7484 | 0.5670 | 23.1 | 18.0 |
| 448 | 0.6419 | 0.3142 | 0.7203 | 0.5915 | 23.5 | 20.3 |
| 480 | 0.6969 | 0.3402 | 0.8782 | 0.5897 | 25.1 | 23.0 |
| 512 | 0.6959 | 0.3343 | 0.7686 | 0.6787 | 24.7 | 25.8 |
| 544 | 0.7482 | 0.3548 | 0.8337 | 0.6861 | 25.0 | 28.7 |
| 576 | 0.7547 | 0.3738 | 0.8811 | 0.6711 | 25.4 | 31.7 |
| **608** | **0.7749** | 0.3825 | 0.8909 | 0.6707 | 25.6 | 35.0 |
| 640 | 0.7743 | **0.3839** | 0.8670 | 0.6814 | 26.4 | 38.3 |

**两个测试集都在 imgsz=608 达到 mAP50 峰值，640 反而略降。** 而 mAP50-95 则一直单调到 640。二者峰值略有错位，说明「能否检出」和「框得准不准」对分辨率的最优值不完全一致。

## 17. 🔴 关键发现：时延由图像解码主导，不由分辨率主导

这是本轮最重要的结论，直接决定第二阶段策略的设计。

**时延拆解（imgsz=320，PKU 图）：**

| 组成 | 耗时 | 占比 |
|---|---|---|
| **JPEG 解码** | **15.6 ms** | **58%** |
| 模型推理 | 5.9 ms | 22% |
| 后处理 (NMS) | 3.5 ms | 13% |
| 预处理 (letterbox/归一化) | 1.1 ms | 4% |
| 其他开销 | ~1 ms | 3% |
| **合计** | **26.9 ms** | |

（解码耗时为实测 `cv2.imread`，因为 ultralytics 的 `speed` 拆解不含图像加载，三项相加只有 10.5ms，缺口就在这里。）

**由此得出三个结论：**

1. **分辨率几乎不是"本地时延"的杠杆。** 320→640 像素翻 4 倍，总时延只从 23.9 → 27.9 ms（+17%），因为模型推理只从 5.9 → 6.6 ms。

2. **但分辨率是极强的"带宽"杠杆。** JPEG 从 22.3 → 67.7 KB（3 倍）。

3. **时延波动主要来自源图尺寸，不是目标分辨率。** test_pcb2025 的最大时延高达 120 ms（而中位仅 25 ms）——因为该数据集源图从 800×600 到 6000×4000 不等，解码成本差异巨大。PKU 源图尺寸统一，时延就非常稳定（23.6~28.2 ms）。

4. **设备端"缩图后再发"几乎免费**：解码 + resize + JPEG 编码共 16.5 ms，比单纯解码（15.6 ms）只多 **0.9 ms**。

> **对第二阶段的意义：自适应策略里，分辨率管"带宽"，放置（本地/边缘）管"时延"。** 若按最初设想用降分辨率来省本地时延，收益极小、精度代价极大（320 时 mAP50 掉到 0.48）。

## 18. 实测 vs 推测（务必区分）

**✅ 实测：**
- PKU 存在 10 块母板、同母板图非缺陷区逐像素相同（像素级比对）
- PKU 第 1 轮划分存在泄漏
- `missing_hole` 与 `missing_pad` 视觉上确为不同缺陷（切图目视）
- PCB-Defect 2025 的 230 张图尺寸全不同
- 所有 mAP / P / R / 时延 / JPEG 字节数
- 时延拆解各分量
- 两测试集峰值在 imgsz=608

**⚠️ 推测（未证实）：**
- **为什么两个测试集都在 608 而非 640 达峰** —— 可能是噪声（test_pcb2025 仅 35 张），也可能是训练/推理尺度效应。需多 seed 才能定论。
- **`mouse_bite` 跨域迁移差的原因**（PS 合成 vs 化学蚀刻）—— 合理但未做对照实验验证。
- 母板分块边界由「相邻序号像素比对」推出，边界处若恰好有极端缺陷可能误判（已用 25px 外扩遮罩降低风险，但未逐块人工复核）。

## 19. 已知局限

1. **test_pcb2025 只有 35 张**，指标方差大，小数点后第二位不必过度解读。
2. **单随机种子**，未做多 seed 重复实验。
3. **mAP50-95 普遍偏低**（0.38~0.40），说明框定位不够精确。
4. PKU 缺陷是**合成的**，PCB2025 是**真实化学蚀刻**的——两者混合训练的结论不能直接外推到产线。
5. unified 模型只在 imgsz=640 训练过一次，未做训练分辨率消融（第 1 轮遗留的 1280 反常问题仍未解释）。

## 20. 复现命令

```bash
python -m pcbvis prepare                      # 数据集 A（PKU）
python -m pcbvis unified                      # 数据集 B + 构建 unified
python -m pcbvis train --data data/unified/data.yaml --name unified_yolo11n
python -m pcbvis profile --weights results/train/unified_yolo11n/weights/best.pt \
    --data data/unified/test_pku.yaml --splits test --tag unified_pku
python -m pcbvis profile --weights results/train/unified_yolo11n/weights/best.pt \
    --data data/unified/test_pcb2025.yaml --splits test --tag unified_pcb2025
python -m pcbvis latency --weights results/train/unified_yolo11n/weights/best.pt \
    --data data/unified/test_pku.yaml --split test --rounds 5
```

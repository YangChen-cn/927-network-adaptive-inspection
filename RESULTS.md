# 训练结果汇总

> 数据集：`RobotHuman/PCB_defect`（PKU-Market-PCB），6 类缺陷，693 张
> 设备：Apple M4 (MPS) | 权重：`results/train/pcb_yolo11n/weights/best.pt`
> 更新：2026-09-18

---

## ⚠️ 请先读这一段：一次数据泄漏及其修正

本项目早期得到过 **mAP50 = 0.9262** 的结果，**该数字因数据泄漏而虚高，已作废。**

### 泄漏是怎么发现的

PKU-Market-PCB **只有 10 块 PCB 母板**，每块母板用 Photoshop 合成 6 类缺陷各若干张。

验证方法：把两张图的缺陷框区域遮掉，只比较**非缺陷区域**的像素。

| 对比 | 平均灰阶差 | 差异像素占比 |
|---|---|---|
| 跨类别·同序号（`missing_hole01` vs `mouse_bite01`） | **0.00** | **0.0%** |
| 同类别·相邻序号（01 vs 02） | **0.00** | **0.0%** |
| 同类别·远隔序号（01 vs 50） | 21.65 | 66.2% |

即 `missing_hole01` 与 `mouse_bite01` **是同一块板的照片**，只是贴了不同缺陷。

早期用的是「按类别文件夹分层随机划分」，结果**同一块母板同时进了 train 和 test**。模型可以靠"认板子"而不是"认缺陷"拿分。

> 母板分块边界（实测，6 个类别目录一致）：21, 41, 51, 61, 71, 81, 91, 96, 106 —— 共 10 块。

### 修正办法

划分改为以**母板 block** 为单位 —— 整块母板只能进一个 split。已固化在 `configs/pcb.yaml` 的 `template_blocks` / `block_split`，并在 `pcbvis/data_prep.py` 的 `split_records()` 里加了**自动泄漏自检**，每次划分完都会打印是否有母板跨 split。

### 泄漏到底虚高了多少（实测对比）

两个模型同为 PKU-only、同一套超参，**同为 val 指标**，只差划分方式：

| 指标 | 泄漏版 | 防泄漏版 | 差距 |
|---|---|---|---|
| mAP50 | 0.9262 | 0.7800 | **+0.1462** |
| mAP50-95 | 0.4886 | 0.3684 | +0.1202 |
| Precision | 0.9416 | 0.8425 | +0.0992 |
| **Recall** | 0.8869 | **0.6384** | **+0.2485** |

**读法：泄漏主要虚高的是召回率（+24.9 个点），不是精确率。** 这符合直觉——模型"认得"见过的板子，于是把本该漏检的缺陷也找了出来；但它并不会因此产生更多误检（精确率只涨了 10 点）。**所以如果只看精确率，泄漏几乎看不出来。**

---

## 1. 数据集

**`RobotHuman/PCB_defect`** —— 即经典 **PKU-Market-PCB**（Huang et al., 2019）。

| 项 | 值 |
|---|---|
| 规模 | 693 张图，2953 个缺陷框（平均 4.26 框/图） |
| 类别 | `missing_hole` `mouse_bite` `open_circuit` `short` `spur` `spurious_copper` |
| 分辨率 | 统一 3034×1586 |
| 标注 | Pascal VOC XML（代码自动转 YOLO） |
| 许可 | MIT，HuggingFace 公开下载，**无需登录** |
| 缺陷来源 | **Photoshop 合成到真实 PCB 照片上** |

### ⚠️ 论文里必须写明的两点

1. **缺陷是合成的**，不是产线真实缺陷，泛化性结论要保守。
2. **该镜像只有 693 张**，约为原数据集（1386 张）的一半。

### 缺陷有多小

实测中位宽度仅占图宽 **2.37%**（3034px 上约 72px），最小 0.79%（约 24px）。letterbox 到正方形后横向缩放比为 `imgsz / 3034`：

| imgsz | 中位缺陷 (72px) | 最小缺陷 (24px) |
|---|---|---|
| 320 | ~7.6 px | **~2.5 px** |
| 640 | ~15 px | ~5 px |
| 1280 | ~30 px | ~10 px |

**最小缺陷在 320 下只剩约 2.5 像素** —— 这解释了为什么低分辨率端精度会崩，也是「分辨率 / 时延 / 带宽」权衡的真实来源。

---

## 2. 防泄漏划分

以母板 block 为单位划分（**整块母板只进一个 split**）：

| split | 母板 | 图片数 | 缺陷框 |
|---|---|---|---|
| train | 1–6 | 480 | ~2045 |
| val | 7–8 | 90 | ~380 |
| test | 9–10 | 123 | 621 |

**自检结果：跨 split 共用的母板 = 无 ✅**

标签格式已独立核验：逐行检查列数、类别 id 范围、坐标 `[0,1]` —— 零问题。

---

## 3. 训练配置与结果

| 项 | 值 |
|---|---|
| 模型 | YOLO11n（COCO 预训练微调） |
| epochs | **100（跑满，patience=30 未触发早停）** |
| imgsz / batch | 640 / 16 |
| 设备 | MPS (Apple M4) |
| 增强 | mosaic 1.0 / scale 0.5 / fliplr 0.5 / close_mosaic 10（ultralytics 默认） |
| 随机种子 | 42 |
| 总耗时 | 98.7 分钟 |

| | epoch | mAP50 | mAP50-95 | P | R |
|---|---|---|---|---|---|
| 首轮 | 1 | 0.0000 | 0.0000 | — | — |
| 10 | 10 | 0.4092 | — | — | — |
| 25 | 25 | 0.6193 | — | — | — |
| 50 | 50 | 0.7162 | — | — | — |
| **最优** | **77** | **0.7800** | **0.3684** | **0.8425** | **0.6384** |
| 末轮 | 100 | 0.7476 | 0.3393 | — | — |

> **关于耗时**：正常轮速 28.5s/epoch，但 100 轮里有 **7 轮出现 6~9 分钟的随机 I/O 卡顿**，占总耗时 55%。
> 已排除：热降频（`pmset -g therm` 明确报告无 thermal warning）、内存（38% 空闲）、磁盘空间。
> **最可能是 Spotlight 索引新建的约 1400 个文件，但未能回溯证实**——仅作推测。精度不受影响（浮点结果是确定的），只影响耗时。

---

## 4. Test 集评估（母板 9–10，123 张 / 621 框，训练中完全未见过）

| 指标 | 值 |
|---|---|
| **mAP50** | **0.8191** |
| mAP50-95 | 0.3554 |
| Precision | 0.8291 |
| Recall | 0.7772 |

> test（0.8191）比 val（0.7800）还高，因为 test 用的是母板 9–10、val 用 7–8，**不同母板难度不同**，两组数字不可直接比较。

### 逐类 mAP50

| 类别 | test mAP50 | |
|---|---|---|
| spur | **0.8856** | ████████████████████████████████████ |
| spurious_copper | 0.8622 | ██████████████████████████████████ |
| open_circuit | 0.8605 | ██████████████████████████████████ |
| mouse_bite | 0.8305 | █████████████████████████████████ |
| missing_hole | 0.7664 | ███████████████████████████████ |
| short | **0.7098** | ████████████████████████████ |

**`short`（短路）最难**（0.710），`spur`（毛刺）最好（0.886）。跨度 17.6 个点，比泄漏版（12 点）更大——说明在真正没见过的板子上，类别间的难度差异才真实显现出来。

---

## 5. 分辨率扫描（test 集，10 个分辨率）

| imgsz | mAP50 | mAP50-95 | P | R | 时延 ms | JPEG KB |
|---|---|---|---|---|---|---|
| 320 | 0.4721 | 0.1455 | 0.5497 | 0.5234 | 25.1 | 22.3 |
| 384 | 0.6717 | 0.2311 | 0.7786 | 0.6510 | 25.4 | 30.3 |
| 416 | 0.7121 | 0.2630 | 0.8697 | 0.6206 | 25.9 | 34.3 |
| 448 | 0.7532 | 0.2709 | 0.8865 | 0.6696 | 26.4 | 38.7 |
| 480 | 0.7772 | 0.2992 | 0.8451 | 0.7318 | 26.9 | 42.8 |
| 512 | 0.8145 | 0.3081 | 0.8400 | 0.7548 | 27.0 | 47.7 |
| 544 | 0.8141 | 0.3239 | 0.8765 | 0.7627 | 27.6 | 52.7 |
| 576 | 0.8363 | 0.3455 | 0.9254 | 0.7539 | 28.1 | 57.6 |
| **608** | **0.8399** | 0.3553 | 0.8911 | 0.7861 | 28.6 | 63.0 |
| 640 | 0.8191 | **0.3554** | 0.8291 | 0.7772 | 28.6 | 67.7 |

曲线图：`results/profile/curves_pku_yolo11n.png`

### 三个结论

1. **峰值在 imgsz=608，不在 640。** mAP50 在 608 达 0.8399，640 反而回落到 0.8191。
   但 mAP50-95 是**单调到 640** 的——说明「能否检出」和「框得准不准」的最优分辨率不一致。
   > ⚠️ 这个 608 峰值**在另一个独立训练的模型上也复现过**（早前的 7 类模型同样在 608 达峰），
   > 所以不太可能纯属噪声。但**仍未做多 seed 验证，属于推测**。

2. **时延几乎不随分辨率变化**：320→640 像素翻 4 倍，中位时延只从 **25.1 → 28.6 ms（+3.5ms）**。

3. **但传输量变化剧烈**：JPEG **22.3 → 67.7 KB（3 倍）**。
   召回率的代价也很大：**0.5234 → 0.7772**。

---

## 6. 🔴 关键发现：时延由图像解码主导，不由分辨率主导

**时延拆解（imgsz=320，PKU 图）：**

| 组成 | 耗时 | 占比 |
|---|---|---|
| **JPEG 解码** | **15.6 ms** | **58%** |
| 模型推理 | 5.9 ms | 22% |
| 后处理 (NMS) | 3.5 ms | 13% |
| 预处理 (letterbox/归一化) | 1.1 ms | 4% |
| 其他开销 | ~1 ms | 3% |
| **合计** | **26.9 ms** | |

解码耗时为**实测**（直接计时 `cv2.imread`）。ultralytics 自带的 `speed` 拆解不含图像加载，三项相加只有 10.5ms，缺口就在这里。

**另外实测：解码 + resize + JPEG 编码共 16.5 ms，比单纯解码（15.6 ms）只多 0.9 ms。**

→ **设备端"缩图后再发送"几乎免费。**

### 对第二阶段（自适应策略）的直接意义

**分辨率管「带宽」，放置管「时延」。**

若按最初设想用降分辨率来省本地时延：320 相比 640 只省 3.5ms，却让 mAP50 从 0.82 掉到 0.47——**收益极小、代价极大，不是一条可行路径**。

而卸载到边缘的价值在于：边缘算力更强 → 可以用高分辨率跑，网络差时又可以把分辨率降下来省传输。**分辨率的正确用途是适配网络，不是适配本地算力。**

---

## 7. 实测 vs 推测（务必区分）

**✅ 实测（有直接证据）：**
- PKU 只有 10 块母板、同母板图非缺陷区域逐像素相同（像素级比对）
- 泄漏版 vs 防泄漏版的指标差距（mAP50 +0.1462，Recall +0.2485）
- 所有 mAP / P / R / 逐类指标
- 所有时延、JPEG 字节数
- 时延拆解的各分量（解码 15.6ms 为直接计时）
- `pmset -g therm` 报告无热降频
- 100 轮中出现 7 次 6~9 分钟的卡顿

**⚠️ 推测（未证实）：**
- **卡顿的成因**：最可能是 Spotlight 索引，但**无法回溯证实**。
- **608 达峰的原因**：可能源于「训练/推理尺度不匹配」（模型只在 640 上训练过），也叠加了 test 集本身的性质。该现象在两个独立训练的模型上复现，但仍**未做多 seed 验证**。
- **类别难度差异的原因**：`short` 最难、`spur` 最易，与缺陷的视觉尺度有关，但未做对照实验。

---

## 8. 已知局限

1. **单数据集、单随机种子**，未做多 seed 重复实验。
2. **test 集只有 123 张 / 621 个框**，指标存在方差，小数点后第二位不必过度解读。
3. **mAP50-95 只有 0.3554**，远低于 mAP50 —— 说明**能检出缺陷，但框定位不够精确**。若论文要报定位精度，需如实说明。
4. **缺陷是 Photoshop 合成的**（见第 1 节）。
5. **绝对时延数字对本机热状态/IO 状态敏感**（已观测到同分辨率下 26ms 与 40ms 的差异）。跨机器比较时需重新标定。

---

## 9. 产物位置

```
results/train/pcb_yolo11n/          ← 防泄漏模型（当前有效）
├── weights/best.pt                 mAP50 0.7800 (val)
├── results.csv                     逐 epoch 指标
├── results.png / confusion_matrix*.png / Box*_curve.png
└── val_batch*_pred.jpg             验证集预测可视化 ← 看框准不准用这个

results/train/pcb_yolo11n_LEAKY_backup/   ← 已作废的泄漏版，仅作对照，可删
results/profile/                           ← 10 个分辨率的精度/时延/字节
results/profile_LEAKY_backup/              ← 泄漏版的对应结果
results/eval/metrics_imgsz640_test.json
```

---

## 10. 复现命令

```bash
python -m pcbvis prepare --no-download    # 重建数据集（含防泄漏划分 + 自检）
python -m pcbvis train                    # 微调
python -m pcbvis eval --split test        # test 集评估
python -m pcbvis profile --splits test    # 10 个分辨率：精度+时延+JPEG字节
python -m pcbvis latency --split test     # 交错法测时延（抗热漂移）
python -m pcbvis predict --imgsz 640      # 推理出带框图
```

> `latency` 用**交错法**（轮询所有分辨率多轮取中位），因为顺序逐个测会被热漂移和 I/O 卡顿污染——实测顺序测时同一分辨率不同轮次能差 50%+。
